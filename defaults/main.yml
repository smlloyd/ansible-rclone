---

rclone_version: "1.38"

# Defaults in case no variables for OS are chosen
rclone_packages:
  - unzip

rclone_executable:
  path: /usr/local/bin/rclone
  owner: root
  group: root

rclone_man_pages:
  owner: root
  group: root
